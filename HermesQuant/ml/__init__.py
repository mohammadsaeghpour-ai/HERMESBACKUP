"""
HermesQuant ML Package
========================
Hybrid Deep Learning + Sklearn Ensemble for Crypto Trading

Modules:
    features.py          — Feature engineering pipeline
    ml_engine.py         — Original ML engine (RF + GB)
    ml_engine_v2.py      — Enhanced ensemble (RF + GB + LightGBM)
    walk_forward.py      — Walk-forward optimization
    
    hybrid_tcn.py        — Temporal Convolutional Network (multi-scale)
    hybrid_transformer.py — Transformer with multi-head attention
    hybrid_cnn_lstm.py   — CNN-LSTM hybrid (pattern + sequence)
    hybrid_gan.py        — GAN for synthetic data augmentation
    hybrid_attention_lstm.py — Attention-weighted LSTM
    
    hybrid_ensemble.py   — Meta-ensemble combining all 5 DL models
    hybrid_agent.py      — Agent integration for 22-agent system
"""
